query GetSearchArtistById($vendorId:String){
  search {
    artist(vendorId:$vendorId) {
      name
      id
      thumbnail
      albums {
        id 
        name
        year
        thumbnail
      }
    }
  }
}
