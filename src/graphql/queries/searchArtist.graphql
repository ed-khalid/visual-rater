query SearchByArtist($name:String){
  search {
    artists(name:$name) {
      name
      vendorId
      thumbnail
      albums {
        vendorId 
        name
        year
        thumbnail
      }
    }
  }
}