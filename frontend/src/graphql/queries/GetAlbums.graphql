query GetAlbums($ids:[String!]!) {
    albums(ids:$ids) {
        id
        artistId
        name
        year
        score
        thumbnail
        thumbnailDominantColors
        genres {
            primary {
                id 
                name
            }
            secondary {
                id 
                name
            }
        }
    }
}